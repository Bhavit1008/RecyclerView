<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recyclerview">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Blogs.Scorpio"></activity>
        <activity android:name=".Blogs.Xuv" />
        <activity android:name=".Blogs.Hector" />
        <activity
            android:name=".Blogs.Brezza"
            android:label="@string/title_activity_brezza"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Blogs.Kia" />
        <activity android:name=".Blogs.Dzire" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>